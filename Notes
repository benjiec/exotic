List properties
If filtering by properties, group/color by property and label
List attributes, include/exclude
Customized columns - e.g. EIC
Output CSV

Example:

sample = datasets
  properties
    - timepoints
    - reactions
    - duplicates
    - subjects
    - buffers
    - samples
    - strains
    - molecules

observation = targets with names

value = peaks
